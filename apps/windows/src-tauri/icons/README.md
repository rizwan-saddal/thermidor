# App Icons

Place the following icon files here before building:

- `32x32.png` — 32x32 PNG
- `128x128.png` — 128x128 PNG
- `128x128@2x.png` — 256x256 PNG (2x)
- `icon.ico` — Windows ICO (multi-resolution)

Generate from a source SVG/PNG via: `pnpm tauri icon path/to/source.png`
