export default function SettingsWindow() {
  return (
    <div>
      <h1 className="page-header">Settings</h1>
      <div className="placeholder-card">
        <p className="page-description">
          Configuration panels for gateway, providers, channels, agents, models,
          security, and advanced options. Mirrors the macOS app's 11-tab
          settings layout.
        </p>
      </div>
    </div>
  );
}
