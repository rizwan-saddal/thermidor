export default function OnboardingFlow() {
  return (
    <div>
      <h1 className="page-header">Welcome to OpenClaw</h1>
      <div className="placeholder-card">
        <p className="page-description">
          First-run onboarding wizard: gateway connection setup (local or
          remote), provider API key configuration, channel pairing, and initial
          agent selection.
        </p>
      </div>
    </div>
  );
}
