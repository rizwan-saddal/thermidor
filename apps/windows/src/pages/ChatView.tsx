export default function ChatView() {
  return (
    <div>
      <h1 className="page-header">Chat</h1>
      <div className="placeholder-card">
        <p className="page-description">
          Agent chat interface with message history, streaming responses,
          thinking indicator, and session management. Mirrors the web UI chat
          experience.
        </p>
      </div>
    </div>
  );
}
